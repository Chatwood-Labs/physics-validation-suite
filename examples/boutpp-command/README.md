# BOUT++ command adapter template

This is a configuration example, not a bundled BOUT++ benchmark and not an
automated acceptance case. Review the command, output filename, variable names
and tolerances for the specific BOUT++ model before running it.

PVS core sees only a command, a NetCDF artefact and generic checks. Scientific
meaning and tolerance policy remain in this case.

